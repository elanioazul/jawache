import { CustomizedpipePipe } from './customizedpipe.pipe';

describe('CustomizedpipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomizedpipePipe();
    expect(pipe).toBeTruthy();
  });
});
